# BBPathInspection
BBPathInspection is an automated web-application reconnaissance tool that maps the unlinked attack surface of any target, the hidden routes, unreferenced API endpoints, leaked source maps, and fuzz-discovered paths that a normal user browsing the site never sees, but that a security tester needs to find.
